{
  "name": "neural-works-pomodoros",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build-server": "cd server && yarn install",
    "build-client": "cd client && yarn install",
    "start-server": "cd server && yarn start",
    "start-client": "cd client && yarn start"
  },
  "dependencies": {
    "if-env": "1.0.4"
  }
}
